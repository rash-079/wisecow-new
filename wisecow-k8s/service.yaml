apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
spec:
  selector:
    app: wisecow
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80       # The port that will be exposed externally
      targetPort: 4499  # The port inside the container that Wisecow listens on
